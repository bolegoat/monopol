Drop country flag PNGs here named: mk.png al.png me.png ba.png xk.png rs.png hr.png si.png
(3:2 ratio works best, e.g. 96x64). Until a PNG exists the built-in SVG flag from
js/board-data.js is shown underneath, so a missing file is never fatal.

xk.png (Kosovo) is a generated placeholder — swap in a real flag when you have one.
bg.png is left over from the Bulgaria group, which the board no longer uses.
